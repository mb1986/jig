# jig

Run commands with arguments taken from a declarative configuration file.

> Placeholder README. The real one lands at the end of v1 — see
> `SPEC.md` and `IMPLEMENTATION.md` for now.

Unix-only (Linux, macOS) for v1.
